{
  "name": "ctufts-blog",
  "private": true,
  "scripts": {
    "dev": "npx @11ty/eleventy --serve --watch --port 8081",
    "build": "npx @11ty/eleventy"
  },
  "devDependencies": {
    "@11ty/eleventy": "^2.0.1"
  }
}
